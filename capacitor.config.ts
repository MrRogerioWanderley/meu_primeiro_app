import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proj_ionic_unisum',
  appName: 'proj_ionic_unisum',
  webDir: 'www/browser'
};

export default config;
